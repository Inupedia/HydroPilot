[project]
name = "hydropilot-core"
version = "0.1.0"
description = "Deterministic hydro models for HydroPilot"
requires-python = ">=3.11"
dependencies = ["pydantic>=2.7"]

[project.optional-dependencies]
test = ["pytest>=8"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
