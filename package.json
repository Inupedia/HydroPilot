{
  "name": "hydropilot",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "setup": "python scripts/setup_dev.py",
    "dev": "npm --prefix apps/desktop run dev",
    "dev:web": "python scripts/dev_web.py",
    "test": "npm --prefix apps/web test -- --run"
  }
}
